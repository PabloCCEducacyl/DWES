<div class="container">
    <header class="py-3 mb-4 border-bottom">
        <nav class="header d-flex flex-wrap flew align-items-center justify-content-md-between justify-content-around">
            <a href="?" class='btn d-flex flex-row align-items-center'><img src="imagenes/importante.gif"><h1>Tienda</h1></a>
            <div>
                <?php
                    //if(isset($_SESSION['email'])){
                        echo "<a class='btn' href='?carrito'>Carrito</a>";
                    /*} else {
                        echo "<a href='?login'>Iniciar Sesión</a>";
                    }*/
                ?>
            </div>
        </nav>
    </header>
</div>